import { shortie } from 'gov-api/helpers/shortie';
import { module, test } from 'qunit';

module('Unit | Helper | shortie');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = shortie([42]);
  assert.ok(result);
});
